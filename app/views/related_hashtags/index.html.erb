<div class="container">
	<%= link_to 'Back', root_path %>
<div ng-app="relatedManager">
  <div ng-controller="RelatedCtrl as r">
    {{ r.test }}
    {{ r.tags }}
<!--     <div ng-repeat="r in tags">
      {{r.name}}
    </div> -->
  </div>
</div>

<div class="bnw">

  <h1> Results for Hashtag Feed:<%=@hashtag_feed.name %> <br></h1>
   <%@hasher.each do |x| %>
     <%= x[0] %> <br> <%= x[1] %> <br>
   <% end %>
</div> 
</div>